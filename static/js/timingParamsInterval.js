//set the timing parameters 


const intervalDurations = [6000,7000,8000,9000]; // length of the inverval in miliseconds
const itiDurations = [1000,1500,2000];  // length of the inverval in miliseconds
const isiDurations = [500,750];  // length of the inverval in miliseconds
const test = true;  // length of the inverval in miliseconds
